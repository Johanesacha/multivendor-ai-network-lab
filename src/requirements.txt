flask==3.1.3
flask-cors==6.0.2
paramiko>=4.0.0
netmiko==4.6.0
requests>=2.33.0
gunicorn==23.0.0
junos-eznc==2.7.6
defusedxml==0.7.1
# app.py calls load_dotenv() at import time to read .env (MVLAB_API_KEY,
# CLI_PROXY_PASSWORD, ANTHROPIC_API_KEY, ...); without this package app.py
# raises ModuleNotFoundError on startup.
python-dotenv==1.2.2
# anthropic SDK powers the /api/mv/orchestrator LLM path (Telegram /ask + AI Command);
# if it's missing the orchestrator silently degrades to offline rule-based diagnosis.
anthropic==0.105.2
cryptography>=49.0.0
